// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDY1UmBRu6jjDOkBEjAhsznZ3YVkgI2u2k",
    authDomain: "modo-997bd.firebaseapp.com",
    databaseURL: "https://modo-997bd.firebaseio.com",
    projectId: "modo-997bd",
    storageBucket: "modo-997bd.appspot.com",
    messagingSenderId: "977404014158",
    appId: "1:977404014158:web:0dda69967842c982"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
